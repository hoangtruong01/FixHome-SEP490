// src/shared/enums/invitation-status.enum.ts
export enum InvitationStatus {
  STANDBY = 'standby',
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  DECLINED = 'declined',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}
